# dependencies
node_modules/

# built engine bundle (regenerated by `npm install` / `npm run build:engine`)
engine/dist/

# local dev artifacts
tmp/
shots/
*.log

# analysis cache also lives in $TMPDIR/vibometr — nothing to commit here
*.local

# os / editor
.DS_Store
.idea/
.vscode/
*.swp
